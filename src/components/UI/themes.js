import { colors } from './variaveis'

export const themes = {
  light: {
    bg: colors.$backgroundLight,
    textColor: colors.$textLight,
    division: colors.$pageDetailsLight
  },

  dark: {
    bg: colors.$backgroundDark,
    textColor: colors.$textDark,
    division: colors.$pageDetailsDark
  }
}
